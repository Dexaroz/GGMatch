package com.pamn.ggmatch.app.architecture.model.profile.preferences

import com.pamn.ggmatch.R

enum class PlaySchedule(val iconRes: Int) {
    MORNING(R.drawable.morning),
    AFTERNOON(R.drawable.afternoon),
    NIGHT(R.drawable.night),
}
